# 📚 Noryx VPN: Развертывание на 3 серверах

> ⚠️ **ВАЖНО:** Это актуальная документация для правильной установки на 3 серверах с RemnaWave Panel

---

## 🎯 Что это такое?

Полная инструкция по развертыванию VPN платформы **Noryx** на **3 серверах** с использованием **RemnaWave Panel**.

### Почему 3 сервера?

RemnaWave Panel использует **встроенный Caddy**, который занимает порты **80 и 443** для автоматического получения SSL сертификатов. Это означает:

- ❌ Нельзя запустить другой веб-сервер на том же сервере
- ❌ Subscription Page не может быть на сервере с RemnaWave
- ✅ **Нужен отдельный 3-й сервер для Subscription Page**

---

## 📖 Документация

### 🏗️ [3_SERVERS_ARCHITECTURE.md](3_SERVERS_ARCHITECTURE.md)

**Начните здесь!** Подробное описание архитектуры:
- Схема всех 3 серверов
- Что где находится
- Как компоненты взаимодействуют
- Требования к серверам
- Настройки безопасности
- Сравнение с другими вариантами

**Для кого:** Всем, кто хочет понять общую картину

---

### 📋 Быстрый старт

Если у вас уже есть опыт с Linux и Docker:

#### Шаг 1: Подготовка

```bash
# Создать DNS записи
servervpn.store        → IP_сервера_1
panels.servervpn.store → IP_сервера_2
sab.servervpn.store    → IP_сервера_3

# Сгенерировать токены
openssl rand -hex 32  # REMNAWAVE_API_TOKEN
openssl rand -hex 32  # SUBSCRIPTION_API_TOKEN
openssl rand -base64 32  # JWT_SECRET
```

#### Шаг 2: Сервер 1 (VPS)

```bash
# Установка
apt update && apt upgrade -y
apt install -y nodejs npm postgresql-15 nginx certbot

# PostgreSQL
sudo -u postgres psql
CREATE DATABASE noryxvpn;
CREATE USER noryxvpn_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE noryxvpn TO noryxvpn_user;

# Проект
cd /var/www
git clone your-repo noryxvpn
cd noryxvpn
npm install --production

# Заполнить .env (см. .env.vps.example)
# Настроить Nginx
# Получить SSL: certbot --nginx

# Запустить
systemctl enable --now noryxvpn
```

#### Шаг 3: Сервер 2 (RemnaWave)

```bash
# Docker
curl -fsSL https://get.docker.com | sh

# RemnaWave
mkdir -p /opt/remnawave && cd /opt/remnawave
curl -o docker-compose.yml https://raw.githubusercontent.com/remnawave/backend/refs/heads/main/docker-compose-prod.yml
curl -o .env https://raw.githubusercontent.com/remnawave/backend/refs/heads/main/.env.sample

# КРИТИЧЕСКИ ВАЖНО: Заполнить .env
nano .env
# FRONT_END_DOMAIN=panels.servervpn.store
# SUB_PUBLIC_DOMAIN=sab.servervpn.store/api/sub
# JWT_AUTH_SECRET=...
# REMNAWAVE_API_TOKEN=... (должен совпадать с Сервером 1!)

# Запустить
docker compose up -d
```

#### Шаг 4: Сервер 3 (Subscription)

```bash
# Docker
curl -fsSL https://get.docker.com | sh

# Проект
mkdir -p /opt/subscription-page/{app,public}
cd /opt/subscription-page

# Создать app/package.json, app/server.js, public/index.html
# Создать docker-compose.yml с Caddy
# Создать .env с SUBSCRIPTION_API_TOKEN (должен совпадать с Сервером 1!)

# Запустить
docker compose up -d
```

#### Проверка

```bash
curl https://servervpn.store/api/health
curl https://panels.servervpn.store/api/health
curl https://sab.servervpn.store/health
```

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────┐
│   СЕРВЕР 1: VPS                     │
│   servervpn.store                   │
│   • Node.js + Express               │
│   • PostgreSQL                      │
│   • Nginx                           │
└─────────────────────────────────────┘
              ↕ HTTPS API
┌─────────────────────────────────────┐
│   СЕРВЕР 2: RemnaWave Panel         │
│   panels.servervpn.store            │
│   • Docker: RemnaWave               │
│   • Встроенный Caddy (80/443)       │
└─────────────────────────────────────┘
              ↕ HTTPS API
┌─────────────────────────────────────┐
│   СЕРВЕР 3: Subscription Page       │
│   sab.servervpn.store               │
│   • Docker: Node.js App             │
│   • Caddy (80/443)                  │
└─────────────────────────────────────┘
```

---

## 📦 Требования

| Сервер | CPU | RAM | Диск | ОС |
|--------|-----|-----|------|-----|
| **Сервер 1** | 2 vCPU | 4 GB | 40 GB | Ubuntu 22.04 LTS |
| **Сервер 2** | 2 vCPU | 2 GB | 20 GB | Ubuntu 22.04 LTS |
| **Сервер 3** | 1 vCPU | 1 GB | 10 GB | Ubuntu 22.04 LTS |

---

## 🔐 Безопасность

### Критически важные настройки

1. **IP Whitelist на Сервере 2:**
   ```bash
   ufw allow from IP_СЕРВЕРА_1 to any port 443
   ```

2. **API токены должны совпадать:**
   - `REMNAWAVE_API_TOKEN` на Сервере 1 и 2
   - `SUBSCRIPTION_API_TOKEN` на Сервере 1 и 3

3. **Firewall на всех серверах:**
   ```bash
   ufw allow ssh
   ufw allow 80/tcp
   ufw allow 443/tcp
   ufw --force enable
   ```

---

## 🐛 Решение проблем

### SSL не получается

```bash
# Проверить DNS
nslookup panels.servervpn.store

# Проверить порты
netstat -tulpn | grep :80

# Логи Caddy
docker compose logs caddy --tail 100
```

### 502 Bad Gateway

```bash
# Проверить backend
systemctl status noryxvpn
journalctl -u noryxvpn -n 50

# Перезапустить
systemctl restart noryxvpn nginx
```

### API не отвечает

```bash
# Проверить токены
grep REMNAWAVE_API_TOKEN .env

# Проверить firewall
ufw status

# Тест API
curl -H "Authorization: Bearer TOKEN" https://api-url/health
```

---

## 📚 Дополнительные файлы

В проекте есть примеры конфигураций:

- `.env.vps.example` - для Сервера 1 (VPS)
- `.env.remnawave.example` - для Сервера 2 (RemnaWave)
- `nginx.conf` - пример конфигурации Nginx
- `database-vps-schema.sql` - схема БД для Сервера 1

---

## ✅ Чеклист развертывания

### Подготовка
- [ ] Есть 3 VPS сервера
- [ ] DNS записи созданы
- [ ] Токены сгенерированы

### Сервер 1 (VPS)
- [ ] Node.js 20 установлен
- [ ] PostgreSQL 15 настроен
- [ ] Nginx настроен
- [ ] SSL получен
- [ ] API работает: `curl https://servervpn.store/api/health`

### Сервер 2 (RemnaWave)
- [ ] Docker установлен
- [ ] `.env` правильно заполнен
- [ ] `FRONT_END_DOMAIN` и `SUB_PUBLIC_DOMAIN` указаны
- [ ] Контейнеры запущены
- [ ] Caddy получил SSL автоматически
- [ ] Панель работает: `https://panels.servervpn.store`

### Сервер 3 (Subscription)
- [ ] Docker установлен
- [ ] Приложение создано
- [ ] `.env` с правильным токеном
- [ ] Контейнеры запущены
- [ ] Страница работает: `https://sab.servervpn.store/health`

### Связь
- [ ] Сервер 1 может обращаться к Серверу 2
- [ ] Сервер 3 может получать данные от Сервера 1
- [ ] Токены совпадают

---

## 🚀 Что дальше?

После успешного развертывания:

1. **Добавить VPN серверы** в RemnaWave Panel
2. **Настроить платежи** (ЮKassa/Stripe)
3. **Протестировать** полный цикл покупки
4. **Настроить мониторинг** (UptimeRobot, Prometheus)
5. **Настроить бэкапы** PostgreSQL

---

## 📖 Официальная документация

- **RemnaWave:** https://docs.rw/
- **PostgreSQL:** https://www.postgresql.org/docs/
- **Caddy:** https://caddyserver.com/docs/
- **Let's Encrypt:** https://letsencrypt.org/

---

## ⚠️ Важные замечания

1. **Не используйте архитектуру на 2 серверах** - это не работает из-за конфликта портов
2. **Следуйте официальной документации RemnaWave** для установки панели
3. **Caddy автоматически получает SSL** - не нужно ничего настраивать вручную
4. **IP Whitelist обязателен** для RemnaWave (Сервер 2)
5. **Токены должны совпадать** между серверами

---

**Документация создана:** 2024-02-17

**Версия:** 3.0 (архитектура на 3 серверах)

**Статус:** ✅ Production ready
